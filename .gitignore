# === OS junk files ===
Thumbs.db
desktop.ini
.DS_Store

# === Editor / IDE ===
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# === PHP / Server config (ຄວນເກັບໄວ້ສ່ວນຕົວ ບໍ່ຄວນເຂົ້າ Git) ===
config.php
config/database.php
.env
conn/db1.php
phpinfo.php
picture/cleanup_duplicates.php

# === Logs ===
*.log
error_log

# === Uploaded files / user content (ຮູບທີ່ user ອັບໂຫລດ - ບໍ່ຄວນເຂົ້າ Git) ===
picture/uploads/
picture/uploads - Copy/
uploads/

# === XAMPP default dashboard (ບໍ່ກ່ຽວກັບ project) ===
dashboard/

# === Duplicate / backup ໄຟລ໌ (ເຊັ່ນ "index - Old.php", "upload - new.php", "x - Copy.php") ===
* - Old.*
* - new.*
* - Copy.*
* - Copy (*).*
*-Old.*
*-Copy.*

# === Composer / vendor (ຖ້າໃຊ້ Composer) ===
/vendor/
composer.lock

# === Node modules (ຖ້າມີ) ===
node_modules/

# === Backup files ===
*.bak
*.old
*~
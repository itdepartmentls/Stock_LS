# === OS junk files ===
Thumbs.db
desktop.ini
.DS_Store

# === Editor / IDE ===
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# === PHP / Server config (ຄວນເກັບໄວ້ສ່ວນຕົວ ບໍ່ຄວນເຂົ້າ Git) ===
config.php
config/database.php
.env

# === Logs ===
*.log
error_log

# === Uploaded files / user content (ຖ້າບໍ່ຢາກໃຫ້ຮູບອັບໂຫລດເຂົ້າ Git) ===
# ຍົກເລີກຄອມເມັນແຖວລຸ່ມນີ້ ຖ້າຕ້ອງການບໍ່ໃຫ້ Git ຕິດຕາມໂຟນເດີຮູບພາບ
# picture/uploads/
# uploads/

# === Composer / vendor (ຖ້າໃຊ້ Composer) ===
vendor/
composer.lock

# === Node modules (ຖ້າມີ) ===
node_modules/

# === Backup files ===
*.bak
*.old
*~